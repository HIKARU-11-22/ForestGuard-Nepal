# Dataset
Download the Kaggle dataset and extract it into `data/raw/`.

Expected structure:
data/raw/
  train/
    fire/
    smoke/
    non_fire/
  test/
    fire/
    smoke/
    non_fire/

If the extracted folder has a different name, update `DATA_DIR` in `src/config.py`.
